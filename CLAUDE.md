# cbc

## Overview
Empty repository — purpose and stack TBD.

## Repository
- GitHub: https://github.com/d3v07/cbc (private)
- Default branch: `main`
- Collaborators: d3v07 (owner), Frex22, PruthviVKadam

## Commands
TBD — populate as `make`/`npm`/`pytest` etc. are added.

## Conventions
- Inherits global rules from `~/.claude/CLAUDE.md` and `~/.claude/rules/`.
- Branch naming: `feat/sprint-N-name`, `fix/...`, `chore/...` — never commit directly to `main`.
- Commits reference issues: `feat: description (Closes #N)`.
- No secrets in code; `.env` is gitignored.

## Status
Initial scaffold — no source code yet.
